#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int n, m;

int main()
{
	cin >> n >> m;
	int **card;
	card = new int*[n];
	for (int i = 0; i < n; i++)
	{
		card[i] = new int[m];
	}
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++)
			cin >> card[i][j];
	}
}
